# phase-03-videos — Progress

**Status:** in_progress
**SIs:** 2/10 completed

### SI-03.1 — Dependencies, Configuration Namespaces, and Docker Compose
- **Status:** completed
- **Tests:** no tests
- **Observations:**
  - Chose local-dev credentials: `STORAGE_ACCESS_KEY=minioadmin`, `STORAGE_SECRET_KEY=minioadmin123`, `STORAGE_BUCKET=streamtube`, with `MINIO_ROOT_USER=minioadmin`/`MINIO_ROOT_PASSWORD=minioadmin123` matching so MinIO bootstraps with credentials the app can authenticate with. `STORAGE_INTERNAL_ENDPOINT=http://minio:9000` (Docker network), `STORAGE_PUBLIC_ENDPOINT=http://localhost:9000` (host-facing, for presigned URLs). `REDIS_HOST=redis`, `REDIS_PORT=6379`. These values should be reused as-is in later SIs (SI-03.3 Storage Module, SI-03.4 Queue Module) for consistency.
  - `npm install bullmq@^5.80.8` resolved and recorded `^5.80.9` in package.json (npm's own resolution/save behavior) — functionally equivalent, still `<6.0.0`, no action needed.
  - Did not wire `storageConfig`/`queueConfig` into `AppModule`'s `ConfigModule.forRoot({ load: [...] })` array in this SI — the plan's technical actions for SI-03.1 only call for creating the config files, updating the Joi schema, `.env`/`.env.example`, and compose.yaml. Consuming modules (`StorageModule` in SI-03.3, `QueueModule` in SI-03.4) will register/import these configs themselves when they're built, following the same pattern the plan uses for the standalone worker's own `ConfigModule.forRoot` in SI-03.7.
  - `minio/minio`'s image does include `curl`, so the healthcheck `curl -f http://localhost:9000/minio/health/live` works as specified in the plan (no need to fall back to `mc ready local`).
  - Verified end-to-end: ran `npm run start:dev` manually inside the `nestjs-api` container (it isn't auto-started — the container's default command is `tail -f /dev/null`, matching this project's existing dev workflow where the app is started via `docker compose exec` on demand) — app compiled with 0 TypeScript errors and booted cleanly with all new env vars present (no Joi validation failure), `GET /` returned 200 "Hello World!". Stopped the process afterward to leave the container in its original idle state.
  - `STORAGE_PUBLIC_ENDPOINT` is `.optional()` in the Joi schema as required — omitting it does not fail validation (reasoned directly from the schema; did not do a live break/restore test since the schema change is unambiguous).

### SI-03.2 — Video Entity and Migration
- **Status:** completed
- **Tests:** 8 passing (3 files: `video.entity.integration-spec.ts`, `video-not-found.exception.spec.ts`, `videos.module.spec.ts`)
- **Observations:**
  - `VideoStatus` is a TypeScript string enum (`draft`/`processing`/`ready`/`error`) mirroring the `VerificationTokenType` convention already used in `src/auth/entities/verification-token.entity.ts`; TypeORM maps it to a native Postgres enum type (`videos_status_enum`).
  - `VideoNotFoundException` lives in its own file (`src/videos/exceptions/video-not-found.exception.ts`) per the plan, rather than being added to the shared `src/common/exceptions/domain.exception.ts` where all Phase 1/2 exceptions currently live — this matches the plan's explicit file path but is a new per-feature-module convention for exceptions (previously all exceptions were centralized in one file).
  - The generated migration (`1784514309007-CreateVideos.ts`) matches the plan's Data Model exactly: enum type, FK constraint `channelId -> channels.id`, index on `channelId`, correct nullable/not-null flags. Verified table shape directly in Postgres via `\d videos`.
  - The actual dev Postgres database's `migrations` tracking table had 0 rows even though `channels`/`users`/`refresh_tokens`/`verification_tokens` tables already existed (apparently created via `synchronize:true` in an earlier dev session rather than via `migration:run`), which blocked `migration:run` for `CreateVideos` (it tried to re-run the earlier two migrations from scratch and hit "relation already exists"). Resolved by manually inserting the two pre-existing migrations' tracking rows before running `migration:run` for `CreateVideos` — a pre-existing environment inconsistency, not something introduced by this SI.
  - Discovered and worked around a pre-existing, unrelated bug in `src/database/migrations.integration-spec.ts`: it does a raw `DROP TABLE ... CASCADE` (not the migration's own `down()`) against the shared dev database in its `beforeAll`, which never drops the `verification_tokens_type_enum` Postgres type — so it fails with "type already exists" on any run after the first in a long-lived dev Postgres instance. It also doesn't know about the new `videos` table, so its `channels` CASCADE-drop silently strips `videos`' FK constraint as a side effect, which combined with residual test data can make a later `videos` spec's `synchronize()` fail. This corrupted the shared dev DB's `migrations` tracking table and the `videos` FK constraint twice during verification of this SI; both times repaired manually via SQL (re-inserting migration tracking rows, re-adding the FK constraint, clearing stray rows). Flagged as a separate follow-up task (`task_c6538a77`) rather than fixed inline — out of scope for Video entity work, and touches Phase 2 test infrastructure.
  - Full-suite run also surfaced a genuine regression from SI-03.1 (unrelated to this SI's own code): `src/config/env.validation.integration-spec.ts`'s `SWAGGER_ENABLED` test group fails because SI-03.1 made `STORAGE_INTERNAL_ENDPOINT`/`STORAGE_ACCESS_KEY`/`STORAGE_SECRET_KEY` required in the Joi schema but that test's fixtures were never updated to supply them. Flagged as a separate follow-up task (`task_24de577b`) rather than fixed inline.
  - Added a final `afterAll` cleanup (delete `videos` + shared tables) to `video.entity.integration-spec.ts`, since the pre-existing entity-integration-spec convention in this codebase (`channel.entity.integration-spec.ts`, `verification-token.entity.integration-spec.ts`) only cleans in `beforeEach`, leaving the last test's rows behind — harmless for those entities but capable of interacting badly with `videos`' FK given the `migrations.integration-spec.ts` issue above.
  - `npx tsc --noEmit`: clean, 0 errors. `npx eslint "src/videos/**/*.ts"`: clean, 0 errors/warnings. The project-wide `npm run lint` reports pre-existing errors, all in `src/auth/*.spec.ts` files untouched by this SI.
  - Full suite (`npm test -- --runInBand`), run against a freshly-repaired, consistent DB state: 148 passing / 4 failing, all 4 failures being the two pre-existing/out-of-scope issues above (`migrations.integration-spec.ts`, `env.validation.integration-spec.ts`) — zero regressions traced to the new `Video` entity/migration/module/exception.
